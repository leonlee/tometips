<ul>
{{#each talents}}
    {{!-- id links not yet working properly and may not even be the best UI
    <li><a href="#talents/{{../type}}/{{toHtmlId id}}{{currentQuery}}" title="{{display_name}}">{{> talent_img_small }}</a></li>
    --}}
    <li><a href="#talents/{{../type}}{{currentQuery}}" title="{{display_name}}">{{> talent_img_small }}</a></li>
{{/each}}
</ul>