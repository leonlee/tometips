{{! FIXME: type header and description }}
{{#each this}}
<h2><a class="anchor" id="talents/{{type}}"></a>{{chnname}}</h2>
<div>
    <p>{{chndescription}}</p>
    <p id="talents/{{type}}-avail"></p>
    <div>
        {{#each talents}}
            <div class="panel panel-default">
                <div class="panel-heading clickable">
                    <h3 class="panel-title">
                        <a id="talents/{{../type}}/{{toHtmlId id}}" data-toggle="collapse" data-target="#collapse-{{toHtmlId id}}">
                            {{> talent_img}}{{chnname}}
                        </a>
                    </h3>
                </div>
                <div id="collapse-{{toHtmlId id}}" class="talent-details panel-collapse collapse">
                    <div class="panel-body">
                        {{> talent_details}}
                        {{#if source_code}}<div class="source-link"><a href="http://git.net-core.org/darkgod/t-engine4/blob/{{tag}}/game/modules/tome/{{source_code.[0]}}#L{{source_code.[1]}}" target="_blank">查看源代码</a></div>{{/if}}
                        </div>
                    </div>
                </div>
            {{/each}}
    </div>
</div>
{{/each}}
